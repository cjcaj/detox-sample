# Sample Expo project with Detox

To get started:
```
# install deps
npm install
npm install -g detox-cli

brew update
brew tap wix/brew
brew install --HEAD applesimutils

# run ios
npm run ios

# run tests
detox test
```
